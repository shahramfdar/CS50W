#This is how you can import a functions.
#from functions import square
#the other way to do this is to import the whole file.
import functions
for i in range(10):
    print(f"the square of {i} is {functions.square(i)}")
